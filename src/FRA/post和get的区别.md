# post 和 get的区别

`GET` 从指定的资源请求数据，并且`请求参数一般显示在URL上`

`POST` 向指定的资源提交要被处理的数据，通过`表单提交`，`不显示在URL上`，更有隐蔽性



`GET` 的请求可以被`缓存`、保留在浏览器历史记录、`有长度限制（URL长度最大2048字符）`、不能用于处理敏感数据、请求只应该获取数据

`POST` `不会被缓存`，不保留记录，对数据长度没有要求